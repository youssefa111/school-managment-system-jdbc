package utils;

public enum Subject {
    ARABIC(1),
    MATH(2),
    ENGLISH(3),
    SCIENCE(4),
    GEOMETRIC(5),
    HISTORY(6);

    final int id;
     Subject(int id){
        this.id = id;
    }
}
